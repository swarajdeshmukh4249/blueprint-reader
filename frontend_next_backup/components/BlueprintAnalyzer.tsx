/**
 * @deprecated Use UploadWorkspace via AppShell on dashboard routes.
 * Kept as a thin alias so legacy imports render the new workspace UI.
 */
export { default } from "@/components/workspace/UploadWorkspace";
