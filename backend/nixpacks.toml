[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "tesseract",
  "poppler_utils",
  "libGL",
  "libGLU",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "python supabase_worker.py"