[phases.setup]
nixPkgs = [
  "python311",
  "tesseract",
  "poppler_utils",
  "libGL",
  "gcc",
  "zlib",
  "libjpeg",
  "libpng",
  "freetype",
  "pkg-config",
]

[phases.install]
cmds = [
  "pip3 install --break-system-packages -r requirements.txt"
]

[start]
cmd = "python3 supabase_worker.py"